import "./Spinner.css";

const Spinner = () => {
    return (
        <div className={"loader-wrapper"}>
            <div className="loader">Loading...</div>
        </div>
    )
};

export default Spinner;